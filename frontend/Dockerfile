# Minimum base image
FROM alpine:latest

# Work directory
WORKDIR /app

# Placeholder: Frontend setup will be added here eventually
CMD ["sh", "-c", "echo 'Frontend placeholder container running' && sleep infinity"]
